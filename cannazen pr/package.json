{
  "name": "cannazen",
  "version": "0.0.0",
  "description": "E-commerce platform with TypeScript, React, and Node.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/aedenmathias5-beep/cannazenPR.git"
  },
  "keywords": ["ecommerce", "typescript", "react", "nodejs", "pnpm"],
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "echo 'Static HTML - no build needed'",
    "start": "echo 'Static HTML - no server needed'",
    "dev": "echo 'Static HTML - no dev server needed'",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
